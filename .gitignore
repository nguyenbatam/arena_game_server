/bin/
/tmp/
/vendor/
*.exe
*.test
*.out
.DS_Store
.env
pprof/
coverage.*
# The working copy of the config; may hold JWT_SECRET / ADMIN_TOKEN.
# backend.env.example is the committed template — see `make env`.
backend.env

# Match recordings (REPLAY_DIR). They are debugging artefacts, sometimes with
# player input in them, and they are regenerated by running the server.
/replays/
*.arnr
